# AI Radar Decision Log

This file captures short accepted decisions. Larger decisions with context, options, and consequences belong in `docs/adr/`.

## Accepted Decisions

1. AI Radar is an event-level AI intelligence product, not a generic news aggregator.
2. The MVP starts from arXiv, Hacker News, and GitHub.
3. The first priority is the smallest real closed loop before LLM analysis, alerts, reports, or evaluation.
4. Backend foundation uses Java 17, Spring Boot 3.5.15, MyBatis-Plus, and Flyway.
5. PostgreSQL is the primary database and `pgvector` is deferred.
6. Immutable `raw_item` snapshots are retained for traceability, reruns, and evaluation.
7. The first clustering baseline is deterministic and rule-based.
8. The first hot-scoring baseline is explainable and rule-based.
9. The first crawl path is manual before introducing scheduler infrastructure.
10. The API contract baseline uses OpenAPI 3.1.
11. The frontend MVP uses Vue 3, TypeScript, Vite, Vue Router, Ant Design Vue, and Axios.
12. The final Phase 3 frontend runs as direct Vue routes instead of `iframe`-wrapped static demo pages.
13. Local text query and minimum-score filtering are explicit frontend display filters, not backend business fields.
14. The first Phase 5 structured analysis path is synchronous, evidence-grounded, and stored in `cluster_analysis` with every success or failure recorded.
15. The first Phase 6 alert path is manual, synchronous, and stored in `subscription_rule` plus `alert_record` before introducing scheduler infrastructure or external delivery channels.
16. The first Phase 7 daily report path is manual, synchronous, and generated from persisted `hot_cluster`, `hot_score`, evidence, and the latest stored `cluster_analysis` before introducing scheduler infrastructure or new model calls.
17. The first Phase 8 evaluation loop is manual, rule-based, and runs labeled cases against persisted data (`raw_item`, `hot_item`, `hot_cluster`, `hot_score`, `cluster_analysis`, `alert_record`) without introducing LLM judges, schedulers, queues, vector stores, or external evaluation platforms.
18. The first fourth-source expansion after Phase 8 is Hugging Face Models, while Hugging Face datasets and Spaces stay out of scope for Phase 9A.
19. The frontend uses a unified `AppLayout` shell component for sidebar/topbar/breadcrumb, with menu items and breadcrumbs derived from route `meta`. UI language is unified to Chinese with ant-design-vue `zh_CN` locale configured via `a-config-provider`. The independent layouts and English copy previously implemented per page (Phase 6/7/8) are consolidated into this shell, and `shared/utils/datetime.ts` provides unified Chinese time formatting.
20. Phase 9A closes with Hugging Face Models integrated into the existing manual crawl-to-cluster path, frontend-compatible source labels, and a dedicated acceptance script plus source connector checklist for future source additions.
21. Phase 10 replaces the Phase 5 fake structured analysis provider with a real OpenAI-compatible provider built on the official `openai-java` SDK and the Chat Completions API. Chat Completions is preferred over the Responses API because it is the lowest-common-denominator OpenAI-compatible surface and is also supported by gateways such as DeepSeek. The wire-level schema mode is `response_format=json_object` rather than the stricter `json_schema`, because the latter is not implemented by every compatible gateway; the JSON shape is described in the system prompt and enforced by `OpenAiAnalysisResponseMapper`. The application must still start when the API key is missing; in that case `/analysis-runs` returns `FAILED` with `ANALYSIS_PROVIDER_NOT_CONFIGURED` persisted in `cluster_analysis`.
22. Phase 11A introduces lightweight scheduled crawl operations with Spring Scheduler, not Quartz or external job infrastructure. The first scheduled loop is limited to source crawling only, reuses `crawl_interval_minutes` plus recent `crawl_task` history for due checks, creates `SCHEDULED` tasks through the existing crawl execution service, and keeps scheduled alert matching, scheduled report generation, external delivery channels, and scheduled evaluation out of scope.
23. Phase 11B extends the lightweight Spring Scheduler baseline to daily report generation. The scheduled runner reuses `DailyReportService.generate(LocalDate)`, targets UTC yesterday by default, skips an existing report unless `refresh-existing` is enabled, and keeps alert matching, external delivery, scheduled evaluation, Quartz, queues, and distributed locks out of scope.
24. Phase 12A introduces the first Chinese platform source via the Tencent Cloud Web Search API (wsa, sourced from Sogou Search), not web scraping. The TC3-HMAC-SHA256 signature is implemented manually in a reusable `TencentCloudV3Signer` utility rather than introducing the Tencent Cloud Java SDK, keeping the dependency footprint unchanged. Config fields align with actual API parameters (`query`, `cnt`, `mode`, `site`, `freshness`), but optional upstream parameters are omitted unless explicitly configured so lite accounts can use the documented query-only request shape. A new `CRAWL_PROVIDER_NOT_CONFIGURED` error code covers missing credentials without blocking application startup, and Tencent Cloud `Response.Error` payloads are treated as crawl upstream failures instead of empty result sets.
25. Phase 12B-1 integrates three low-risk JSON/API sources (Weibo Hot Search, Hacker News Algolia Search, Twitter) to validate heterogeneous API support without introducing heavy infrastructure. Twitter uses `twitterapi.io` via environment variable; when `AI_RADAR_TWITTER_API_KEY` is missing, the application still starts and crawls return `CRAWL_PROVIDER_NOT_CONFIGURED` instead of treating it as an empty result. Weibo Hot Search defaults to `includeTopWhenNoMatch=false` to avoid noise when query does not match the current hot list. Hacker News Algolia Search is kept separate from the existing `HACKER_NEWS` source to avoid semantic confusion between Firebase Top Stories and keyword search with different pagination, filtering, and freshness controls. HTML search sources (Bing, Google, DuckDuckGo) and Bilibili are deferred to later phases to keep 12B-1 focused on stable JSON APIs.
26. Phase 12B-2 integrates HTML search sources (Bing, DuckDuckGo) using jsoup for lightweight HTML parsing, without introducing Playwright/Puppeteer/Selenium browsers, proxy pools, or CAPTCHA handling. Each HTML source is independent with separate client, config validation, collector, and normalizer to keep failures isolated. jsoup 1.18.3 is the only new dependency. Blocked pages (403/429/CAPTCHA/challenge) are detected via keyword matching and mapped to explicit `CRAWL_UPSTREAM_ERROR` rather than treated as empty results. Default `maxAttempts=1` and `minRequestInterval=10s` avoid aggressive retries. Google Search is excluded from 12B-2A due to higher anti-crawl risk and reserved for optional 12B-2B only if local live probes succeed; default configs should be disabled. HTML sources use `WEB_PAGE` item_type, rank-based points (`max(1, totalCount - rank + 1)`), and host/market/region tags. Config fields include required `query`, `limit` (1-20), `freshnessDays` (1-30), and source-specific `market`/`safeSearch` (Bing) or `region` (DuckDuckGo). DuckDuckGo's `uddg` redirect parameter is decoded to extract real URLs before sanitization.
27. Phase 13A freezes the V1 engineering baseline before introducing V2 signals, clustering, and scoring. All Phase 11A, 11B, 12A, 12B-1, and 12B-2 acceptance scripts pass (156 backend tests, 17 frontend tests, build success). Acceptance documentation is updated to reflect Accepted status for all phases. Roadmap and README status descriptions are aligned. V1 clustering (hn-rule-v1 via `RuleBasedClusterService`) and V1 scoring (hn-score-v1 via `RuleBasedScoringService`) behavior is preserved as a replayable baseline. This phase focuses on documentation alignment and verification without introducing algorithm changes or new infrastructure.
28. Phase 13B introduces a minimal signal layer with `SourceRole`, `NormalizedSignal`, and `SourceSignalAdapter` to distinguish source semantics before V2 scoring. Current V1 metrics (`points`, `commentsCount`) are preserved as compatibility fields. Three initial source roles are defined: `ADOPTION` (GitHub, HuggingFace), `COMMUNITY` (HackerNews), and `DISCOVERY` (Bing, DuckDuckGo Search). The signal adapter registry follows the existing pattern of `CollectorRegistry` and `HotItemNormalizerRegistry` with duplicate detection and explicit error handling. This layer prepares for Score V2 while keeping V1 scoring unchanged.
29. Phase 14 introduces signal snapshot storage and 24h growth trend calculation without changing V1 sorting or scoring. `observed_at` uses `raw_item.fetched_at` (crawl-centric time tracking) rather than `published_at` (content publication time) to focus on crawl-to-crawl signal changes. Growth calculation uses a 3-hour maximum deviation tolerance for finding historical snapshots and returns explicit confidence levels (`HIGH`, `MEDIUM`, `LOW`, `UNKNOWN`, `DATA_ANOMALY`, `METRIC_RESET`). Pipeline integration ensures snapshot writes fail the current crawl if unsuccessful, preventing silent data loss. Phase 14 is scoped to 24h window only and does not introduce frontend visualization, multi-window support, or complex cluster aggregation. To avoid regressions once snapshot persistence became mandatory, signal adapters were filled out for all currently implemented source types rather than only the initial 13B subset.
30. Phase 15 introduces `cross-source-score-v2` as a shadow score persisted alongside `hn-score-v1`, without replacing the default ranking. V2 aggregates seven weighted dimensions (momentum 0.25, adoption 0.15, relevance 0.15, freshness 0.15, discussion 0.10, authority 0.10, evidenceDiversity 0.10) so cumulative scale and current growth are scored independently — an established 100k-star project can score high on adoption but low on momentum, while a brand-new project can do the opposite. The existing `RuleBasedScoringService` is wrapped unchanged by `HnScoreV1Strategy`; `ScoringOrchestrator` runs V2 in a `REQUIRES_NEW` transaction and swallows V2 failures so the crawl pipeline is never blocked by a shadow-scoring bug. Search sources (Bing/DuckDuckGo/Sogou) pointing at the same canonical URL count as one DISCOVERY evidence to prevent inflating diversity via redundant discovery entries. No Flyway migration is needed because `hot_score` already allows multiple `scoring_version` rows per cluster; comparison is exposed via `GET /api/v1/hot-clusters/{id}/scores`.
31. Phase 16A introduces a minimal, replayable clustering evaluation baseline before V2 clustering ships. Fixtures are in-memory and frozen (no external dataset, no LLM judges) and split into must-merge and must-not-merge cases. The runner is strategy-agnostic so the same fixture exercises both `hn-rule-v1` and `event-rule-v2`. Aggregate metrics are limited to precision, recall, false-merge count, and false-split count; per-case failure detail is emitted alongside the aggregate report so errors can be triaged without rescanning the fixture. Phase 16A does not change V1 clustering behavior and does not introduce frontend visualization.
32. Phase 16 upgrades URL-level merging to event-level clustering V2 while keeping V1 (`hn-rule-v1`) available and unchanged. V2 follows a deterministic pipeline: `ItemFeatureExtractor` -> `ClusterCandidateRetriever` -> layered match rules -> assignment decision -> persisted reason. Level 1 accepts on deterministic identifiers (canonical URL, arXiv id, GitHub repo, Hugging Face model id); Level 2 requires same core entity + organization + compatible event type + close event time; Level 3 uses a weighted similarity score (`titleSimilarity * 0.30 + entityOverlap * 0.35 + keywordOverlap * 0.15 + actionConsistency * 0.15 + timeProximity * 0.05`) with thresholds `>=0.82 ACCEPTED`, `<0.60 REJECTED`, in between `REVIEW_REQUIRED`. The first version does not use pgvector, embeddings, or LLM as a decision maker; entity resolution relies on source-structured fields, external ids, configuration dictionaries, regex, and alias tables. Candidate retrieval is bounded (default 72h window, max 50 candidates) to avoid full-table scans. All decisions (ACCEPTED, REJECTED, REVIEW_REQUIRED, NO_CANDIDATE) are persisted to `cluster_match_decision` with `match_reason` and `rule_version`. V2 is wired through `ClusterAssignmentOrchestrator`, which supports `ai-radar.cluster.strategy` plus an optional `ai-radar.cluster.shadow-strategy`, mirroring the Phase 15 shadow pattern. Cluster merge/split governance is explicitly deferred to a later phase.

## Pending Decisions

1. When to introduce `pgvector` or other embedding-based retrieval.
2. When scheduling needs exceed the lightweight Spring Scheduler baseline introduced in Phase 11A.
3. When global frontend state is large enough to justify Pinia.
4. When aggregate analytics APIs justify charts or dashboards.
5. Which delivery channel should be introduced first after the manual Phase 6 alert baseline.
6. When automated cluster merge/split governance (resolving REVIEW_REQUIRED decisions, splitting mis-merged clusters) justifies a dedicated Phase 17 backend after Phase 16 V2 ships.

## ADR Index

- [ADR-001: Use PostgreSQL as the Primary Database](adr/ADR-001-primary-database.md)
- [ADR-002: Retain Immutable Raw Item Snapshots](adr/ADR-002-raw-item-retention.md)
- [ADR-003: Start with a Rule-Based Clustering Baseline](adr/ADR-003-rule-based-clustering-baseline.md)
- [ADR-004: Start with Explainable Rule-Based Hot Scoring](adr/ADR-004-rule-based-hot-scoring-baseline.md)
- [ADR-005: Use Vue 3 for the Frontend MVP](adr/ADR-005-frontend-mvp-stack.md)
- [ADR-006: Use OpenAI Chat Completions API with Official openai-java SDK for Real Structured Analysis](adr/ADR-006-real-llm-provider.md)
- [ADR-007: Use Spring Scheduler for the First Lightweight Scheduled Operations Loop](adr/ADR-007-lightweight-scheduled-operations.md)
- [ADR-008: Event Cluster V2 with Deterministic Layered Matching](adr/ADR-008-event-cluster-v2.md)
